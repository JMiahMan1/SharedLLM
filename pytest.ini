[pytest]
asyncio_mode = auto
markers =
    local_only: requires a real local workspace or nested local process behavior; do not run in CI
    server_only: requires the deployed stack or server-specific environment
filterwarnings =
    ignore::DeprecationWarning
norecursedirs = test/legacy_root test/integration test/live
pythonpath = .
