import CalendarApp from '../components/calendar/CalendarApp';

const Calendar = () => {
  return (
    <div style={{ background: '#f5efe3', minHeight: '100%' }}>
      <CalendarApp />
    </div>
  );
};

export default Calendar;
