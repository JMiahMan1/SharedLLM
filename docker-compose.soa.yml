networks:
  sharedllm_internal:
    driver: bridge

volumes:
  identity_db:
  chroma_data:
  logging_data:

services:
  identity:
    build: ./services/identity
    container_name: sharedllm_identity
    restart: unless-stopped
    networks:
      - sharedllm_internal
    ports:
      - "8001:8001"
    env_file:
      - .env
    environment:
      - IDENTITY_DATABASE_URL=sqlite:////data/identity.db
      - INTERNAL_SECRET=${INTERNAL_SECRET:-change-me-in-production}
      - FERNET_KEY=${FERNET_KEY} # Must be provided!
    volumes:
      - identity_db:/data
      # Mount the legacy .env file so the seed script can read it on first run
      - ./.env:/app/.env.legacy:ro

  execution:
    build: ./services/execution
    container_name: sharedllm_execution
    restart: unless-stopped
    networks:
      - sharedllm_internal
    ports:
      - "8003:8003"
    env_file:
      - .env
    environment:
      - INTERNAL_SECRET=${INTERNAL_SECRET:-change-me-in-production}
      - REDIS_URL=redis://redis:6379/0

  rag:
    build: ./services/rag
    container_name: sharedllm_rag
    restart: unless-stopped
    networks:
      - sharedllm_internal
    ports:
      - "8004:8004"
    environment:
      - INTERNAL_SECRET=${INTERNAL_SECRET:-change-me-in-production}
      - CHROMA_PERSIST_DIR=/data/chroma_db
    volumes:
      - chroma_data:/data

  gateway:
    build: ./services/gateway
    container_name: sharedllm_gateway
    restart: unless-stopped
    networks:
      - sharedllm_internal
    ports:
      - "11435:8002" # Only the Gateway is exposed externally
    env_file:
      - .env
    environment:
      - INTERNAL_SECRET=${INTERNAL_SECRET:-change-me-in-production}
      - IDENTITY_SVC_URL=http://identity:8001
      - EXECUTION_SVC_URL=http://execution:8003
      - RAG_SVC_URL=http://rag:8004
      - STORAGE_SVC_URL=http://storage:8005
      - LOGGING_SVC_URL=http://logging:8006
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OLLAMA_URL=${OLLAMA_URL:-http://ollama:11434}
      - FAST_PATH_THRESHOLD=${FAST_PATH_THRESHOLD:-0.85}
    volumes:
      - ./data/phrasebook.json:/app/data/phrasebook.json:ro

  logging:
    build: ./services/logging
    container_name: sharedllm_logging
    restart: unless-stopped
    networks:
      - sharedllm_internal
    ports:
      - "8006:8006"
    environment:
      - INTERNAL_SECRET=${INTERNAL_SECRET:-change-me-in-production}
    volumes:
      - logging_data:/app/data

  storage:
    build: ./services/storage
    container_name: sharedllm_storage
    restart: unless-stopped
    networks:
      - sharedllm_internal
    ports:
      - "8005:8005"
    environment:
      - INTERNAL_SECRET=${INTERNAL_SECRET:-change-me-in-production}

  redis:
    image: redis:7-alpine
    container_name: sharedllm_redis
    restart: unless-stopped
    networks:
      - sharedllm_internal
    ports:
      - "6379:6379"

  automation:
    build: ./services/automation
    container_name: sharedllm_automation
    restart: unless-stopped
    networks:
      - sharedllm_internal
    environment:
      - REDIS_URL=redis://redis:6379/0
      - EXECUTION_SVC_URL=http://execution:8003
      - INTERNAL_SECRET=${INTERNAL_SECRET:-change-me-in-production}
    depends_on:
      - redis
      - execution
