# AGENTS.md

## Temporary Directory Rule

ALWAYS use `.tmp` (relative to workspace root) for temporary work. NEVER use `/tmp` or `/var/folders/.../T/opencode`.

All scratch files, test scripts, and intermediate artifacts must be written under `.tmp/` within the workspace.
